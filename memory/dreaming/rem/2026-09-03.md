# REM Sleep

### Reflections
- No strong patterns surfaced.

### Possible Lasting Truths
- # 2026-09-02 ## HR Weather → Announcement automation (planning) User (HR) asked me to plan an OpenClaw automation for the HR assistant bot. Workflow: - Bot checks weather every 30 min. On bad weather, sends advisory to **HR Weather Drafts** (Telegram chat `-5368977850`) with details (weather, typhoon name, location, dress-down recommendation) + buttons "Compose a draft" / "Discard". - "Compose Draft" → bot writes a formal/professional email advisory → buttons "Send to ABC Employees" / "Edit" / "Discard". - "Send to ABC Employees" → sends to **ABC Employee Announcement** (`-5324314507`). - "Edit" → HR replies with the edited text; bot composes that instead. - "Discard" → discard. - After s [confidence=0.71 evidence=memory/2026-09-02.md:1-20]
- Bot: AblazeHRAssistantBot. Token already present in `~/.openclaw/openclaw.json` under `channels.telegram.accounts["hr-assistant"].botToken` (do NOT copy it into memory/notes). Bindings route `hr-assistant-agent` → telegram account `hr-assistant`. ### Current config state (as of today) - Telegram channel ON, two accounts: `ablaze` (TestAblazeHRAssistantBot, tokenFile `C:\Users\markmb\bot2.token`) and `hr-assistant` (the real Ablaze bot, inline botToken). - `customCommands` already has `check_weather`. - `hr-assistant` account already has group `-5428263875` (allowFrom 8603728634, requireMention false) + wildcard `*` requireMention true. - New target IDs from HR: HR Drafts `-5368977850`, ABC [confidence=0.71 evidence=memory/2026-09-02.md:16-30]
