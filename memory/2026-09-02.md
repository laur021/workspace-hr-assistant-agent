# 2026-09-02

## HR Weather → Announcement automation (planning)

User (HR) asked me to plan an OpenClaw automation for the HR assistant bot. Workflow:

- Bot checks weather every 30 min. On bad weather, sends advisory to **HR Weather Drafts** (Telegram chat `-5368977850`) with details (weather, typhoon name, location, dress-down recommendation) + buttons "Compose a draft" / "Discard".
- "Compose Draft" → bot writes a formal/professional email advisory → buttons "Send to ABC Employees" / "Edit" / "Discard".
- "Send to ABC Employees" → sends to **ABC Employee Announcement** (`-5324314507`).
- "Edit" → HR replies with the edited text; bot composes that instead.
- "Discard" → discard.
- After send, bot asks "Stop monitoring today" / "Continue monitoring".
- "Stop" pauses routine 30-min updates for the day, but escalation (worse weather) still breaks through.
- Manual commands: `/check_weather` (manual query, reply still has compose-draft button), `/create_announcement` (manual announcement creation).

Bot: AblazeHRAssistantBot. Token already present in `~/.openclaw/openclaw.json` under `channels.telegram.accounts["hr-assistant"].botToken` (do NOT copy it into memory/notes). Bindings route `hr-assistant-agent` → telegram account `hr-assistant`.

### Current config state (as of today)
- Telegram channel ON, two accounts: `ablaze` (TestAblazeHRAssistantBot, tokenFile `C:\Users\markmb\bot2.token`) and `hr-assistant` (the real Ablaze bot, inline botToken).
- `customCommands` already has `check_weather`.
- `hr-assistant` account already has group `-5428263875` (allowFrom 8603728634, requireMention false) + wildcard `*` requireMention true.
- New target IDs from HR: HR Drafts `-5368977850`, ABC Employees `-5324314507` (need to be added to `hr-assistant` account groups + bot added to both groups).

### Open decisions (pending HR answers)
1. Location/region for weather + typhoon source (likely Philippines; PAGASA?).
2. "Bad weather" threshold + dress-down criteria.
3. Confirm bot added to both groups (admin), group IDs correct.
4. "Stop monitoring" escalation semantics.
5. Timezone for "today" reset.

### Progress (built + tested)
- Weather source: Open-Meteo (no key); severity 0-3 (thunderstorm/heavy rain/strong gusts). Location = Metro Manila, tz Asia/Manila. Advisory fires at severity>=2 on first detection or escalation (no same-level spam).
- Files: `scripts/hr-weather-check.mjs` (deterministic check: fetch→decide→send advisory with buttons→update state), `scripts/hr-send.mjs` (robust send via outbox, no shell quoting), `state/hr-weather.json` (state), AGENTS.md section "HR Weather → Announcement Workflow" (interactive callback/command handling).
- Automation job `HR weather check` (id c2dc1781-fad4-41e3-89f1-2cba4cdcfc38): cron */30, isolated script payload, delivery none. Verified runs ok.
- Live test: advisory sent to HR Drafts (messageId 24), severity 3.

### IMPORTANT — group IDs migrated (Telegram supergroup upgrade)
- HR Weather Drafts: old `-5368977850` → **`-1003702195046`** (confirmed via migration error + logs). Updated in config/scripts/AGENTS.md.
- ABC Employee Announcement: old `-5324314507` — likely also migrated to a `-100…` id; **need current id from HR** (not yet resolved).

### Remaining
- Get ABC Employees current group id (blocker).
- Verify interactive callback flow (Compose/Send/Edit/Discard/Stop/Continue) when HR taps buttons.
- Hardening: move plaintext `botToken` in openclaw.json to a tokenFile (audit flag).

### FIXED (09:02) — compose draft button failing
Diagnosis: group button callbacks are gated by the group policy; before the 08:56 config reload, HR Drafts matched the wildcard `*` with `requireMention: true`, so callbacks (not mentions) were blocked → "This action is no longer available". Also an external config change (openai plugin + model default) triggered a hot reload that aborted in-flight turns (the "No reply was generated" errors).

Fixes applied:
- **ABC Employee Announcement real id: `-1004452958432`** (found via sessions_list; it also migrated). Updated in config (`channels.telegram.accounts.hr-assistant.groups`), scripts, AGENTS.md.
- AGENTS.md: replaced all stale IDs, added rule: after outbox send reply exactly `NO_REPLY` (fixes /check_weather double reply: buttoned message + extra text summary).
- Reset HR Drafts group session (`agent:hr-assistant-agent:telegram:group:-1003702195046`) so it reloads corrected AGENTS.md (old session context still had `-5368977850` and wasted tokens deliberating).
- Config change detected by gateway; hot reload deferred until active gateway requests drain (applies after the turn).

Next: user re-tests Compose a draft on a fresh advisory message. If still failing, check the reload completed (`openclaw channels logs` → "config hot reload applied") and callback delivery.

## 09:48 Weather Update cron (b5edac43)
- Job payload still references pre-migration HR Drafts ID -5368977850; sent to correct -1003702195046 (messageId 69). Job runs every 20s - high cadence, possibly a test; leave enabled.

## 09:50 Weather Update cron (b5edac43) - second run
- Sent pure weather advisory (sample-format, no buttons) to HR Drafts -1003702195046 (messageId 70). Data: light drizzle now, thunderstorms forecast (17.5 mm, gusts to 85 km/h), severity 3. State not changed (pure advisory). Job still references old ID in payload; keep sending to migrated ID.

- Button expiry root cause: `scripts/hr-send.mjs` and `scripts/hr-weather-check.mjs` sent `action: { type: 'callback', value: ... }`. OpenClaw encodes that form as a plugin-owned opaque callback; with no registered interactive handler, Telegram receives “This action is no longer available.” Changed both to plain button `value` callbacks so the HR agent receives `callback_data: <value>`.
- `/check_weather` duplicate was the model's normal final status after the outbox delivery. AGENTS.md now explicitly requires the only visible result to be the outbox advisory and the final literal `NO_REPLY`; a fresh group session/test is needed to confirm the model follows it.

- Instruction-only `NO_REPLY` did not reliably prevent the duplicate final message. Added the local `hr-single-delivery-guard` OpenClaw hook plugin. After `hr-send.mjs` successfully posts to HR Drafts, it writes a short-lived, session-scoped marker; the plugin cancels only the next normal agent reply for that exact group session. This preserves the advisory and its buttons while removing the redundant status summary. Plugin is loaded with `reply_payload_sending` priority 100 and the gateway was restarted successfully on 2026-09-02.
- Follow-up: payload cancellation caused Telegram to render OpenClaw's fallback “No reply was generated”. Changed the guard to rewrite the redundant final payload to `NO_REPLY` instead, which OpenClaw treats as an intentional silent completion. Gateway restarted and Telegram HR assistant reconnected.
- Correction: `reply_payload_sending` runs after silent-token parsing, so rewriting there exposed literal `NO_REPLY` and still produced the fallback. Replaced that hook entirely with `before_dispatch` for the exact `/check_weather` command. It directly fetches weather and invokes the outbox sender, then returns `{ handled: true }`; OpenClaw documents this as a handled, no-text completion, so no model/fallback reply should be generated. Gateway restarted with the hook loaded.
- Direct handler initially failed because it used synchronous child processes: `hr-send.mjs` opens a local gateway WebSocket, but `spawnSync` blocked the gateway event loop that needed to accept it, producing an opening-handshake timeout. Converted both child invocations to asynchronous `spawn` promises; gateway restarted and HR Telegram plus direct weather lookup were verified working.
- User does not want the redundant “Draft composed and preview sent” status after pressing Compose Draft; the preview is itself sufficient. Extended the direct `before_dispatch` workflow to intercept `compose_draft`, build/update `state.draft`, send only the draft preview with Send/Edit/Discard buttons, and return handled with no model completion. Gateway restarted and Telegram connected.
- User also does not want the “Sent to ABC Employees. Stop monitoring today, or continue?” follow-up or its two monitoring buttons. Extended the direct `send_employees` handler to send the saved draft to the employee group, clear the consumed draft state, and return silently without posting anything to HR Drafts. Gateway restarted and HR Telegram connection verified.


## 15:33 Memory flush — current durable state
- User requested the old deterministic automation “HR weather check” be removed; it was removed successfully.
- The active automation is “Weather Update” (job id `b5edac43-ef73-4973-a703-5eebc2fd92f2`), enabled on a 30-minute cadence for the HR weather workflow.
- User’s desired interaction flow: advisory has one Compose Draft button; composing produces an email-style draft with Send to Employees / Edit / Discard; sending targets ABC Employee Announcement; editing lets HR submit revised copy before sending.
- `/check_weather` was re-added as a manual test command with the same flow as the automation.
- HR Drafts current migrated Telegram ID: `-1003702195046`. ABC Employee Announcement current migrated ID: `-1004452958432`; the older `-5324314507` is stale.
- User does not want duplicate status messages, follow-up monitoring buttons, or processing/tool-call progress in Telegram. `channels.telegram.streaming.mode` was set to `off`; the UI preference `ui.prefs.chatShowToolCalls` was set to `false`. Gateway reload/restart was used as needed.
- A direct workflow hook/plugin was added to prevent duplicate `/check_weather` output and suppress redundant Compose/send confirmations; gateway restart and Telegram reconnection were verified.

## 15:33 Memory flush — maintenance announcement flow
- Added the electrical-maintenance Telegram workflow to `plugins/hr-single-delivery-guard/index.js` with isolated state in `state/hr-maintenance.json`.
- Supported command aliases are `/create_maintenance_announcement` and `/Create_Maintenance_Announcement`; the handler is case-insensitive.
- Flow uses dedicated callbacks: `compose_maintenance_draft`, `send_maintenance_employees`, `edit_maintenance`, and `discard_maintenance`.
- Maintenance messages target HR Weather Drafts (`-1003702195046`) and approved employee notices target ABC Employee Announcement (`-1004452958432`).
- Added the workflow documentation to `AGENTS.md`. The supplied IBM Plaza annual electrical preventive-maintenance wording is preserved in the template, with the schedule formatted for Telegram.
- A gateway restart/reload was in progress when the session was interrupted; verify the running gateway has loaded the updated plugin/config before claiming the command is live.
